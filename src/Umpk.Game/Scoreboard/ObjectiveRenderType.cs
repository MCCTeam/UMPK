namespace Umpk.Game.Scoreboard;

/// <summary>How an objective's scores are rendered on the wire: integer = 0 and hearts = 1.</summary>
public enum ObjectiveRenderType
{
    /// <summary>Scores render as integers.</summary>
    Integer = 0,

    /// <summary>Scores render as hearts.</summary>
    Hearts = 1,
}
